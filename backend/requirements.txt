fastapi==0.135.3
uvicorn[standard]==0.44.0
pydantic[email]==2.12.5
pandas==3.0.2
openpyxl==3.1.5
python-multipart==0.0.26
python-dotenv==1.2.2

# PDF Generation (Day 12)
reportlab==4.4.10

# Authentication (Day 13)
PyJWT[crypto]==2.12.1
bcrypt>=5.0.0
sqlalchemy==2.0.49
psycopg2-binary==2.9.11
alembic==1.18.4

# Billing (Sprint 362)
stripe==15.0.1

# Email Service (Sprint 57)
sendgrid==6.12.5

# Rate Limiting (Sprint 25)
# See docs/compliance/DEPENDENCY_DECISIONS.md for maintenance status
slowapi==0.1.9
limits>=5.8.0

# Redis — shared rate-limit storage across workers/instances (optional)
# Required only when REDIS_URL is set; in-memory fallback otherwise.
redis>=5.0.0

# APM / Error Tracking (Sprint 275)
sentry-sdk[fastapi]==2.57.0

# ODS (OpenDocument Spreadsheet) support (Sprint 432)
odfpy>=1.4.1

# PDF Table Extraction (Sprint 427)
pdfplumber>=0.11.0

# DOCX (Word Document) table extraction (Sprint 597)
python-docx>=1.1.0

# Safe XML parsing — defuses XXE / billion laughs (Sprint 470)
defusedxml>=0.7.1

# Prometheus Metrics (Sprint 435)
prometheus_client>=0.22.0

# Background Scheduler (Sprint 307)
APScheduler==3.11.2

# NOTE: numpy and chardet are transitive dependencies only (via pandas/pdfplumber).
# No direct imports exist — pip resolves them automatically. No pin needed.

# SECURITY: Pinned transitive dependency patches (pip-audit CVEs)
cryptography>=46.0.6  # CVE-2026-26007
pillow>=12.2.0  # CVE-2026-25990
werkzeug>=3.1.8  # CVE-2026-27199
pyasn1>=0.6.3  # CVE-2026-30922 (DoS via recursive ASN.1 decoding)
pypdf>=6.9.2  # CVE-2026-31826, CVE-2026-33123 (memory exhaustion via crafted PDF)

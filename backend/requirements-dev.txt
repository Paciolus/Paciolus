-r requirements.txt

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=6.0.0
freezegun>=1.5.0
hypothesis>=6.100.0
psutil>=7.2.2
httpx>=0.28.0

# Linting
ruff>=0.15.10

# Type checking
mypy>=1.20.1
# PyJWT ships inline type stubs — no separate types package needed
# bcrypt ships inline type stubs — no separate types package needed

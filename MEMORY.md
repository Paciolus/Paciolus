# MEMORY.md — Redirect

> **This file is a stub.** Historical references to `MEMORY.md` in archive files and older protocols
> should be understood as pointing to `SESSION_STATE.md` (session state and progress tracking).
>
> For agent persistent memory, see the Claude Code auto-memory system at
> `~/.claude/projects/<project>/memory/MEMORY.md`.

See [`SESSION_STATE.md`](SESSION_STATE.md) for current session state.

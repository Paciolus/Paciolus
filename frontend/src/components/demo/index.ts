/**
 * Demo-surface components (Sprint 707).
 *
 * These components provide the "forensic instrument" signature moments
 * on the /demo page and homepage demo preview. They are scoped to the
 * demo surface and must not be consumed by real product views — the
 * mechanical gauge, scanline sweep, and margin annotations are
 * performance-affordances, not reporting primitives.
 */

export { ScanlineOverlay } from './ScanlineOverlay'
export { MechanicalGauge } from './MechanicalGauge'
export { MarginAnnotation } from './MarginAnnotation'

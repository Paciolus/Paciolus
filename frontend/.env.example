# =============================================================================
# Paciolus Frontend Configuration
# Sprint 24: Production Deployment Prep
# =============================================================================
# Copy this file to .env.local and configure for your environment.
# NEVER commit .env.local files to version control!
# =============================================================================

# =============================================================================
# REQUIRED - Application will not build/run without this
# =============================================================================

# Backend API URL (no trailing slash)
# This is embedded at build time, so different deployments need different builds.
#
# Development (local):
NEXT_PUBLIC_API_URL=http://localhost:8000
#
# Development (Docker):
# NEXT_PUBLIC_API_URL=http://backend:8000
#
# Staging:
# NEXT_PUBLIC_API_URL=https://api-staging.paciolus.com
#
# Production (Render):
# NEXT_PUBLIC_API_URL=https://paciolus-api.onrender.com
#
# Production (DigitalOcean):
# NEXT_PUBLIC_API_URL=https://api.paciolus.com
#
# Production (Custom Domain):
# NEXT_PUBLIC_API_URL=https://api.paciolus.com

# =============================================================================
# VERCEL DEPLOYMENT
# =============================================================================
# When deploying to Vercel, set these in the Vercel dashboard:
# Project Settings > Environment Variables
#
# Required:
#   NEXT_PUBLIC_API_URL = https://your-backend-url.com
#
# The NEXT_PUBLIC_ prefix makes the variable available in the browser.
# Without it, the variable is only available server-side.

# =============================================================================
# BUILD CONFIGURATION
# =============================================================================

# Disable Next.js telemetry (optional)
# NEXT_TELEMETRY_DISABLED=1

# Node environment (set automatically by Next.js)
# NODE_ENV=production
